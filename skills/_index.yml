generated: '2026-09-04'
method: searched
source: https://github.com/APImetrics/APImetrics-cli/tree/develop/skills/embed
note: >-
  These are the provider's OWN Agent Skills, saved verbatim from the skills/embed directory of the
  first-party APImetrics CLI repository (develop branch) — not generated by API Evangelist. They are
  CLI-grounded rather than OpenAPI-grounded: each step is an `apimetrics` command with an explicit
  validation gate, hard rules and an error-recovery table. Frontmatter (name, description) is the
  provider's; the `cli` and `operations` fields below are our index of what each skill drives.
skills:
- file: apicontext-setup-api-monitor.md
  name: setup-api-monitor
  description: >-
    Set up an API monitor (HTTP call) in APImetrics: create the monitor, attach or create a schedule,
    run on-demand, and verify the result.
  cli: apimetrics
  commands: [create-call, list-schedules, add-call-to-schedule, create-schedule, run-call, list-call-results]
  backing_operations: [create-call, list-schedules, add-call-to-schedule, create-schedule, run-call-v2, list-results-by-call]
- file: apicontext-setup-browser-monitor.md
  name: setup-browser-monitor
  description: >-
    Set up a browser monitor in APImetrics: create the monitor, attach or create a schedule, run
    on-demand, and verify the result.
  cli: apimetrics
  backing_operations: [create-browser-monitor, list-schedules, create-schedule, run-monitor]
- file: apicontext-setup-mcp-monitor.md
  name: setup-mcp-monitor
  description: >-
    Set up an MCP monitor in APImetrics: create the monitor with session steps, attach or create a
    schedule, run on-demand, and verify the result.
  cli: apimetrics
  backing_operations: [create-mcp-monitor, list-schedules, create-schedule, run-monitor]
